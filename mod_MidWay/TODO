
 * reattach if mwd dies and restarts while apache does not.

 * missing content-length -> ap_set_content_length()

 * location directives, should we be able attach to different mwd instances
   from different locations? Yes, but we really need support in client
   API for concurrent atatch to multiple instances.
   Reconsider this with domain thought in networking.

 * timeout directives.?
